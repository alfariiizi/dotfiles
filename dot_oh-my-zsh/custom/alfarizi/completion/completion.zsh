# === Autocomplete ===
# Loaded before sourcing oh-my-zsh.sh
# This script automatically loaded on .zshrc
# Checkout the .zshrc file for more detail

# [dotfiles] This is my own script to handle dotfiles
fpath=($fpath $HOME/bin/dotfiles)
export PATH=$PATH:$HOME/bin/dotfiles

# [others]
